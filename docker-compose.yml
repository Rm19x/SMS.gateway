version: '3'
services:
  redis:
    image: redis:alpine
  web:
    build: .
    command: bundle exec rackup --host 0.0.0.0 -p 4567
    volumes:
      - .:/app
    ports:
      - "4567:4567"
    depends_on:
      - redis